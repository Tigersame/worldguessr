{
  "name": "farcastbase-smart-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Farcastbase reward token",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:base": "hardhat run scripts/deploy-token.js --network base",
    "deploy:base-sepolia": "hardhat run scripts/deploy-token.js --network baseSepolia",
    "verify": "hardhat verify --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.4.5"
  }
}

